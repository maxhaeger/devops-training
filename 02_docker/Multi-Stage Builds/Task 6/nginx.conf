events {}
http {
    include mime.types;

    server {
        listen 80;
        server_name localhost;

        root /data/www;
        index simple.html;

        location / {
            try_files $uri $uri/ =404;
        }
    }
}